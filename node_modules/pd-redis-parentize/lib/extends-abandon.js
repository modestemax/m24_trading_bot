'use strict';
var ptNomen = require('./nomenclt');

module.exports = function (Parent, Child) {
    Parent[ptNomen.abandon(Child)] = function (parentSid, childSid) {
        return Child[ptNomen.abandon(Parent)](childSid, parentSid);
    };
};
