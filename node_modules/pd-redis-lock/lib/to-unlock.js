'use strict';
var redisMaker = require('pd-node-redis');
var defaultRedis = redisMaker.via();

module.exports = function (LockName, inputRedis) {
    var redis = inputRedis || defaultRedis;
    return redis.exec('del', LockName);
};