'use strict';

module.exports = function (Record, uniqDef, id) {
    var setKey = Record.uniqNm.uniqSetOf(uniqDef);
    return Record.redis.exec('zscore', [setKey, id]).then(function (sid) {
        return sid;
    });
};
