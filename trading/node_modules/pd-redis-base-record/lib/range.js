'use strict';
var q = require('q');
var _ = require('underscore');

module.exports = function (Record, option) {
    return Record.rangeSidList(option).then(function (sidList) {
        return Record.findBySidList(sidList, option);
    });
};
